title: The Sumner Lab
description: Insect Research Group, University College London
url: "https://www.sumnerlab.co.uk"
lang: en-GB

# Cloudflare Web Analytics site token (public identifier, safe to commit --
# not the same as the secret API token used to read data back out).
# See /numbers/ for setup notes. Leave blank to keep analytics disabled.
cloudflare_analytics_token: ""

markdown: kramdown
permalink: /:title/

plugins:
  - jekyll-feed
  - jekyll-sitemap
  - jekyll-seo-tag
  - jekyll-redirect-from

collections:
  people:
    output: true
    permalink: /people/:slug/

defaults:
  - scope:
      path: ""
      type: "people"
    values:
      layout: "person"
  - scope:
      path: ""
      type: "posts"
    values:
      layout: "post"
  - scope:
      path: ""
    values:
      layout: "page"

exclude:
  - Gemfile
  - Gemfile.lock
  - README.md
  - vendor
